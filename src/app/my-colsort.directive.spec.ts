import { MyColsortDirective } from './my-colsort.directive';

describe('MyColsortDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColsortDirective();
    expect(directive).toBeTruthy();
  });
});
